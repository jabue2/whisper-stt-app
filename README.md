# Whisper STT App

This is a Whisper STT app. (WIP)

Currently, it only runs directly via python.

Once started, you can start activate it using a set button combination and then stop again, after speaking. It will then transcribe the speech and save the transcribed text to the clipboard and sends a notification.